name: Testing actions

on:
    push:
     branches:
      - 'main'
    pull_request:
     branches:
      - 'main'

jobs:
    test1:
     runs-on: ubuntu-latest

     steps:
      - name: step1
        run: echo "hello world"
	
